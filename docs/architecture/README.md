# Architecture Docs

Architecture decisions and system design documents live here.

Use `ARCH-000-template.md` as the starting point for new architecture documents.

Naming convention: `ARCH-NNN-short-title.md` (e.g., `ARCH-001-mcp-server-design.md`).
